engines:
  eslint:
    enabled: true
    channel: 'eslint-10'
    config:
      config: 'eslint.config.mjs'

ratings:
  paths:
    - '**.js'

checks:
  method-complexity:
    config:
      threshold: 10
